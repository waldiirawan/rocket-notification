describe('Test Init rocket', () => {
    it('should call generator function and return string', () => {
        // const rocket = require('../../src/')
        // new rocket(`${__dirname}/../config/rocket.js`)
    })
})
